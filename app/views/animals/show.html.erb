<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Animal</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to animals_path, class: button_class(theme: "transparent") do %>
      All Animals
      <% end %>
      <%= link_to edit_animal_path(@animal), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
      <li>
        <p class="font-semibold">Name:</p>
        <p><%= @animal.name %></p>
      </li>
      <li>
        <p class="font-semibold">Dateofbirth:</p>
        <p><%= @animal.dateofbirth %></p>
      </li>
      <li>
        <p class="font-semibold">Age:</p>
        <p><%= @animal.age %></p>
      </li>
      <li>
        <p class="font-semibold">Characteristic:</p>
        <p><%= @animal.characteristic %></p>
      </li>
      <li>
        <p class="font-semibold">Food:</p>
        <p><%= @animal.food %></p>
      </li>
      <li>
        <p class="font-semibold">Category:</p>
        <p><%= @animal.category.specy %></p>
      </li>
  </ul>
</div>
